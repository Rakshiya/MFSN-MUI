import { Puff } from  'react-loader-spinner'
import {
  CSpinner,
} from "@coreui/react";
const LoaderComponent = () => {
  return (
  <CSpinner/>
  );
};
export default LoaderComponent;


// spinner types
// Audio, BallTriangle, Bars, Circles, CirclesWithBar, FallingLines, Grid, Hearts, InfinitySpin, LineWave, MutatingDots, Oval, Puff, RevolvingDot, Rings, RotatingLines, RotatingSquare, TailSpin, ThreeCircles, ThreeDots, Triangle, Watch